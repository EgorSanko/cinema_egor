# Полный переход с TMDB на HDRezka — план миграции (все поверхности)

> Ветка `hdrezka-migration`. Цель: **убрать TMDB полностью** во всём продукте — сайт, «Вместе», свайп, ТВ-обёртка, мобильные приложения. Вести весь каталог/метаданные от HDRezka.
> Прод (`revamp`) не трогаем — вливаем «по щелчку», когда весь чек-лист зелёный.
> Обновлено 2026-07-02 после research HDRezka + решений владельца.

## Решения владельца (2026-07-02)
1. **История/избранное — ПЕРЕНОСИМ** (не сбрасываем). Фоновый маппинг `tmdb_id → hdrezka_id`: при первом входе для каждого старого id один раз резолвим название→HDRezka→id, кэшируем. Никто ничего не теряет.
2. **Роутинг карточек — оставляем `/movie/[id]` и `/tv/[id]`** (id теперь = HDRezka numeric id). Минимум переделок, SEO-структура цела. (решил за владельца по его просьбе)
3. **MovieTok (`/feed`) — УДАЛЯЕМ ПОЛНОСТЬЮ.** «Никому не зашла, бесполезна.» → не мигрируем, вырезаем.
4. Охват — **весь продукт**, не только сайт: сайт + «Вместе» + свайп + ТВ + приложения. Фаз должно быть много; по ходу вычищаем ненужное.

---

## Решения по фичам (2026-07-02)
**ВЫРЕЗАЕМ:**
- ❌ **MovieTok / `/feed`**: `app/feed/`, `app/api/feed/`, `app/api/feed/events/`, `components/feed/*`, `lib/feed/*`, ссылка в `navbar.tsx`, трейлер-функции в `lib/tmdb.ts` (если не нужны в trailer-modal).
- ❌ **`/compare`** (сравнение двух фильмов, ссылка из профиля) — владелец не пользуется → вырезать (`app/compare/`, ссылка в `app/profile/page.tsx`, ветка в `api/sync`). *(подтвердить)*

**ОСТАВЛЯЕМ и мигрируем на HDRezka:**
- ✅ **Годовые итоги** (`/wrapped`, `/status`) — «крутая фишка». На HDRezka-жанрах+рейтингах.
- ✅ **Актёры** (`/person/[id]`) — HDRezka отдаёт полноценно (фото/био/фильмография).
- ✅ **Подборки** = **это те же жанры/коллекции HDRezka** (`/films/<genre>/`, `/collections/`). Отдельной TMDB-работы НЕТ — `lists`/`list` переводим на HDRezka-жанры/коллекции.

---

## Вывод research: HDRezka отдаёт ВСЁ нужное (и местами больше)
`Player.post.info` уже экспонирует наша либа `hdrezka`:
| Данные | Атрибут | Пример |
|---|---|---|
| Описание/ориг.имя/год | `description`,`orig_title`,`release` | ✓ |
| Жанры/страны/хронометраж | `genre`(Hyperlink→/films/<slug>/), `country`, `duration` | ✓ |
| Постер | `poster` | full+preview (statichdrezka.ac → через /hd-img) |
| Рейтинги | `ratings` | imdb 8.4/767398, kp 8.18/227196, hdrezka |
| Актёры/режиссёры | `persons`,`directors` | Person(id,url,name,**image,birthday,birthplace,career,height**) → полные /person/ |
| Возраст | `age_rating` | AgeRating(age=12, …) |
| Коллекции/франшиза/топы | `collections`,`post.franchises`,`rankings` | тематические + все части серии + «Лучшая фантастика 2024 №1» |
| Трейлер | `POST /engine/ajax/gettrailervideo.php {id}` | `<iframe youtube.com/embed/<YT_ID>>` → YouTube id |
| imdb tt-id | `ratings.imdb.url` | → skip-segments (интро/аутро) продолжат работать |

Витрина (подтверждено, 45 карт/стр, `?page=N`): `/films/`,`/series/`,`/cartoons/`,`/animation/` (новинки категории), `/new/` (всё новое), `/films/best/` (популярное), `/films/best/<genre>/<year>/` (лучшее по жанру+году), `?filter=watching` (смотрят сейчас), жанровые под-каталоги, `/collections/`.

---

## Поверхности и footprint TMDB (что заменить/вырезать)
**A. Сайт (repo `movie/`)** — home, `/genres`, `/movie/[id]`, `/tv/[id]`, `/person/[id]`, `/collections`, `/list`, `/lists`, `/status`, `/wrapped`, `/favorites`, `/history`, `/profile`, `/search`, `/compare`. Ядро TMDB — `lib/tmdb.ts` (35), `tv-player`/`movie-player`, `hero-section`, `genre-grid`, `recommendations`, `collections-row`, `continue-watching`, `schema.ts` (SEO), `achievements`, `status`, `storage`.
**B. «Вместе» (watch-together)** — `app/watch`, `watch/create` (5), `watch/[code]/watch-client` (4), `/cast` (3), `send-to-tv` (1), `api/tv-room`. Комната + синхро-просмотр по коду.
**C. Свайп** — `app/swipe`, `swipe/room/[code]/room-client` (5), `components/swipe-cards` (7), `api/swipe` (5). Тиндер-подбор (сейчас на TMDB discover/жанрах).
**D. ТВ-обёртка (web `/tv-*`)** — `tv-home` (27!), `tv-watch`, `tv-search`, `tv-hd`, `tv-login`, `tv-app`, `components/tv/*`.
**E. Приложения (ОТДЕЛЬНЫЕ репо) — сузили после аудита 2026-07-02:**
- ✅ **`kinotv-app`** — нативное RN телефонное приложение (`sapkefly.apk`, собран app-release.apk 98МБ) со СВОИМ TMDB (`src/api/tmdb.ts`). **Единственное, что нужно реально мигрировать** (свой api-слой → те же `/api/*` HDRezka).
- ✅ **`kinotv-webview`** — Android WebView-обёртка сайта (`sapkeflykino-tv.apk`, TV). Своего TMDB НЕТ → миграция веба `/tv-*` (трек D) покрывает; только пересобрать+переподписать APK.
- ⛔ **`kinotv-tv`** (нативное RN multi-TV, экспериментальное), **`KinoTV`** (старый Android), **`online-cinema`** (Django) — НЕ в проде («не знаю откуда») → игнорируем/архив.

---

## Новые эндпоинты бэкенда (kino-api `hdrezka_server.py`)
Кэш как у `/api/find`/`/api/details`, лёгкие read-only фетчи (бокс 4ГБ — без тяжёлого).
1. `GET /api/browse?cat=&sort=latest|popular|watching|best&genre=&year=&page=` → карточки витрины.
2. `GET /api/card?url=` (расширить `/api/details`) → полная карточка (ratings/persons/directors/collections/franchise/rankings/age/genres/…).
3. `GET /api/trailer?url=` → `{youtube_id}`.
4. `GET /api/person?url=` → актёр + фильмография.
5. `GET /api/franchise?url=` / `GET /api/collection?url=` → части серии / тематическая коллекция.
6. `GET /api/genres` → фикс-список жанров HDRezka (name+slug).
7. `GET /api/map?tmdb=&type=` → фоновый маппинг tmdb_id→hdrezka (для миграции истории). Кэш навсегда.
Уже есть: `/api/find` (поиск), `/api/episodes` (сезоны/серии), `/api/resolve` (стрим по url).

---

## Фазы (много, с acceptance на каждой). Всё за флагом `NEXT_PUBLIC_SOURCE=hdrezka`, TMDB как fallback до флипа.

### Трек 0 — Фундамент
- **Ф0.1** Бэкенд-эндпоинты §1-6 + кэш. ✅ curl каждого на 5 типах тайтлов (фильм/сериал/аниме/мультсериал/новинка) → валидный JSON.
- **Ф0.2** Общий слой данных фронта: `lib/source.ts` с тем же интерфейсом, что `lib/tmdb.ts`, но от HDRezka; флаг переключения. ✅ Юнит-проверка соответствия форм.
- **Ф0.3** Картинки: `hdImg(url)` вместо `getImageUrl`; nginx `/hd-img/` ок. ✅ постеры грузятся у РФ-клиента.

### Трек 1 — Вырезать ненужное (делаем РАНО, чтобы не мигрировать мёртвое)
- **Ф1.1** Удалить MovieTok `/feed` целиком (роуты, компоненты, lib/feed, ссылка в navbar). ✅ сайт собирается, ссылок на /feed нет, 404 на /feed.
- **Ф1.2** Ревью кандидатов (compare/lists/status/wrapped/person) с владельцем → вырезать помеченное. ✅ список утверждён.

### Трек 2 — Сайт: карточка и плеер (самое ценное)
- **Ф2.1** `/movie/[id]` от HDRezka (описание/рейтинги/актёры/жанры/возраст/похожее/франшиза). ✅ Playwright 10 фильмов.
- **Ф2.2** `/tv/[id]` от HDRezka (+ сезоны/серии/озвучки — уже есть). ✅ 10 сериалов+аниме+мультсериалы.
- **Ф2.3** Плеер: прямой резолв по hdrezka_url (без угадывания). ✅ 0 «не то/не найдено» на выборке 50.
- **Ф2.4** Трейлер (модалка) от HDRezka. ✅ открывается на 10 тайтлах.

### Трек 3 — Сайт: витрина и навигация
- **Ф3.1** Главная (`page.tsx`, hero, ряды) на `/api/browse`. ✅ только смотрибельное, клик→играет (50/50).
- **Ф3.2** Жанры (`/genres`, genre-grid, `/list`) на HDRezka-жанрах. ✅ каждый жанр отдаёт карточки.
- **Ф3.3** Бесконечный скролл/пагинация. ✅ подгрузка страниц.
- **Ф3.4** Рекомендации/«похожее»/коллекции/франшизы. ✅ на карточке есть блок, ведёт на играбельное.

### Трек 4 — Сайт: поиск, люди, статистика, SEO
- **Ф4.1** Поиск 100% HDRezka (добить `unified.ts`). ✅ топ-20 запросов релевантны.
- **Ф4.2** `/person/[id]` от HDRezka (если не вырезали). ✅ актёр→его фильмы.
- **Ф4.3** Статистика/достижения/wrapped на HDRezka-жанрах+рейтингах (если не вырезали). ✅ считается.
- **Ф4.4** SEO `schema.ts`/sitemap на HDRezka-данных/URL. ✅ Rich Results Test, нет битых TMDB-ссылок.

### Трек 5 — «Вместе» (watch-together)
- **Ф5.1** `watch/create`, `watch/[code]`, `/cast`, `send-to-tv`, `api/tv-room` на HDRezka-метаданных. ✅ создать комнату→смотреть вместе фильм+сериал, каст на ТВ.

### Трек 6 — Свайп
- **Ф6.1** `swipe-cards`, `swipe/room`, `api/swipe` — подбор из HDRezka-каталога/жанров вместо TMDB discover. ✅ свайп в одиночку + комната мэтчинга работают.

### Трек 7 — ТВ-обёртка (web `/tv-*`)
- **Ф7.1** `tv-home` (главная/ряды), `tv-search`, `tv-watch`, `tv-hd` на HDRezka. ✅ D-pad: главная/поиск/карточка/плеер (бокс/фильм/сериал/аниме).

### Трек 8 — Данные пользователей + блок-лист
- **Ф8.1** Фоновый маппинг tmdb→hdrezka для истории/избранного/позиций/статусов. ✅ старый юзер видит свою историю на HDRezka-карточках.
- **Ф8.2** Блок-лист takedown только по slug/hdrezka_id. ✅ заблокированное = 404 + вне выдачи.

### Трек 9 — Приложения (сужено до одного живого)
- **Ф9.1** `kinotv-app` (RN, телефон): переписать `src/api/tmdb.ts` на HDRezka `/api/*` эндпоинты (browse/card/trailer/person/search), картинки через /hd-img, id=hdrezka. ✅ APK: главная/карточка/плеер/поиск от HDRezka, пересобран (gradlew), залит в /var/www/downloads/.
- **Ф9.2** `kinotv-webview` (TV APK): своего кода-данных нет — после трека D пересобрать+переподписать (kinotv-release.keystore/KinoTV2026), залить. ✅ TV-APK грузит новый `/tv-*`.
- ~~kinotv-tv / KinoTV / online-cinema~~ — не в проде, не трогаем.

### Трек 10 — Флип
- **Ф10.1** Убрать TMDB-код/прокси-зависимость из всех поверхностей; смёржить в `revamp`; выкатить (webpack-деплой). ✅ финальный чек-лист.

---

## Чек-лист флипа (не переключаемся, пока не всё зелёное)
- [ ] Сайт: главная/жанры/поиск/карточка(фильм+сериал+аниме+мультсериал)/плеер/трейлер/актёры/похожее/франшиза — HDRezka, 0 обращений к api.themoviedb.org.
- [ ] Клик по любой карточке витрины → играет (нет тупиков).
- [ ] «Вместе», свайп — на HDRezka.
- [ ] ТВ-обёртка `/tv-*` — на HDRezka.
- [ ] Приложения (живые) — на HDRezka.
- [ ] MovieTok удалён; вырезанные фичи убраны чисто.
- [ ] История/избранное старых юзеров перенесены (маппинг).
- [ ] Блок-лист takedown действует.
- [ ] Постеры грузятся у РФ (через /hd-img).
- [ ] SEO без битых TMDB-ссылок.
- [ ] Прод `revamp` не задет до мержа; откат = вернуть флаг/ветку.

## Остаётся уточнить с владельцем
1. Подтвердить вырез **`/compare`** (MovieTok уже решён).
2. Старт: **Ф0 (эндпоинты) → Ф1 (вырез MovieTok+compare) → Ф2 (карточка)**. Ок?

## Резюме объёма после решений
- Вырезаем: MovieTok, compare (мелочь).
- Оставляем/мигрируем на HDRezka: сайт (карточка/витрина/поиск/жанры/рекомендации), годовые итоги, актёры, «Вместе», свайп, ТВ-обёртка `/tv-*`.
- Приложения: только `kinotv-app` (нативное) реально мигрируем; TV-APK — пересборка после веба; 3 репо игнор.
